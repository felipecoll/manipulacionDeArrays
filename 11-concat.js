const animals = ['ant', 'bison', 'camel', 'duck', 'elephant'];
const otherAnimals = ['perro', 'gato', 'comadreja', 'vibora']

const unionArrays = animals.concat(otherAnimals);

console.log(unionArrays)